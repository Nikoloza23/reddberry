export const reduxSkills = (store) => store.skills;
export const identity = (store) => store.identity;
export const covid_list = (store) => store.covid_list;
export const event = (store) => store.event;
